import type { SettingsConfig } from './types';
/** Remove AI provider secrets before sending settings to the Electron bot client. */
export declare function sanitizeBotSettings(settings: SettingsConfig): SettingsConfig;
//# sourceMappingURL=sanitizeBotSettings.d.ts.map